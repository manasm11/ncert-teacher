# Supabase — project URL and anonymous key (required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Ollama Cloud — API credentials for LLM inference (required)
OLLAMA_CLOUD_API_KEY=your_ollama_api_key
OLLAMA_CLOUD_ENDPOINT=your_ollama_endpoint

# SearXNG — self-hosted search engine for web search node (optional)
# If not set, web search falls back to a placeholder response.
# SEARXNG_URL=http://localhost:8080
